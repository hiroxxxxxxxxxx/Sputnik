---
description: Sputnik プロジェクトのルール（re-export 禁止・改善案はユーザ判断）
globs:
alwaysApply: true
---

# プロジェクトルール

- **re-export 禁止**: シンボルは定義元モジュールからだけ import する。別モジュールで同じ名前で再公開（re-export）しない。利用側は定義元から直接 import すること。
- **改善案は勝手に実装しない**: 改善案を提示した場合は、どの案で・どこまでやるかユーザの判断を求める。ユーザが案を選ぶまで実装しない。
